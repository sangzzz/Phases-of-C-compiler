#include <stdio.h>
int main()
{
    int var;
    if ()
    {
        printf("%s", "UPPERCASE LETTER");
    }
    else if (var >= 'a' && var <= 'z')
    {
        printf("%s", "LOWERCASE LETTER");
    }
    else
    {
        printf("%s", "SPECIAL CHARACTER");
    }
    printf("%d", var);
}

/*
TEST CASE 9
*/

// This test case has no condition inside the if statement.
// Error

// FAIL -> LINE NO. 4